export enum ProductStatus {
  Normal = 'normal',
  Draft = 'draft',
  War = 'war',
}

export enum ProductStatusFilter {
  All = 'all',
  Normal = 'normal',
  Draft = 'draft',
  War = 'war',
}

export enum SeoStatusFilter {
  All = 'all',
  Filled = 'sudah',
  Empty = 'belum',
}

export enum MediaType {
  Image = 1,
  Video = 2,
}
